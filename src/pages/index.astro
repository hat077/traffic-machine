---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';

// This is the "Frontmatter" (Server-side JavaScript)
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<style>
			/* Minimalist "Hacker" CSS */
			.hero {
				padding: 4rem 0;
				text-align: center;
				background: #f4f4f5;
				border-bottom: 1px solid #e4e4e7;
			}
			.hero h1 { font-size: 3rem; margin-bottom: 0.5rem; letter-spacing: -1px;}
			.hero p { font-size: 1.25rem; color: #52525b; max-width: 600px; margin: 0 auto; }
			
			.grid-container {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
				gap: 2rem;
				padding: 3rem 1rem;
				max-width: 960px;
				margin: 0 auto;
			}

			.card {
				border: 1px solid #e4e4e7;
				border-radius: 8px;
				padding: 1.5rem;
				transition: transform 0.2s;
				text-decoration: none;
				color: inherit;
			}
			.card:hover {
				transform: translateY(-5px);
				box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
				border-color: #000;
			}
			.card h3 { margin-top: 0; font-size: 1.5rem;}
			.card span { font-size: 0.9rem; color: #666; text-transform: uppercase; letter-spacing: 1px; font-weight: bold;}
		</style>
	</head>
	<body>
		<Header />
		
		<main>
			<section class="hero">
				<h1>Welcome to Explainova</h1>
				<p>Clear answers for the digital age. We provide honest software comparisons, transparent salary data, and career roadmaps.</p>
			</section>

			<div class="grid-container">
				</div>

			<section style="max-width: 720px; margin: 0 auto; padding: 0 1rem;">
				<h2>Why Explainova?</h2>
				<p>
					The digital world is noisy. Trying to find out how much a tool <em>actually</em> costs or which certification <em>really</em> leads to a job can feel impossible amidst all the marketing fluff.
				</p>
				<p>
					<strong>Explainova</strong> exists to cut through the noise. We build data-driven guides and unbiased comparisons to help you make smarter decisions about your tools, your budget, and your future.
				</p>
			</section>
		</main>

		<Footer />
	</body>
</html>